# Bitcoin App – External REST API Integration

Flask-based Bitcoin / crypto app using the public CoinGecko API for:
- Live prices (`GET /coins/live-prices`)
- Detailed coin info (`GET /coins/<id>/external-info`)

External docs, error handling, and test instructions are included in this project.
